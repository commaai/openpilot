Import('envB', 'added_libs', 'cython_dependencies', 'arch')

envB.SharedLibrary('clock.so', source='clock.pyx', LIBS=added_libs)
envB.SharedLibrary('params_pyx.so', source='params_pyx.pyx', LIBS=added_libs)
