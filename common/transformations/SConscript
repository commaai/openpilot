Import('envB', 'added_libs', 'cython_dependencies', 'arch')
from Cython.Build import cythonize
cythonize('transformations.pyx')
envB.Program('transformations.so', 'transformations.cpp', LIBS=[] + added_libs)
