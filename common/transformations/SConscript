Import('env')

transformations = env.Library('transformations', ['orientation.cc', 'coordinates.cc'])

libtransformations = env.Program('libtransformations_c.so', ['transformations.cpp', 'transformations_capi.cc'],
                                 LIBS=[transformations])
Export('transformations', 'libtransformations')
