name: tools
on:
  push:
  pull_request:

jobs:
  plotjuggler:
    name: plotjuggler
    runs-on: ubuntu-20.04
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    - name: Install plotjuggler snap
      run: sudo snap install plotjuggler
    - name: Unit test
      run: |
        tools/plotjuggler/test_plotjuggler.py

